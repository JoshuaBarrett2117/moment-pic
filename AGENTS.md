# AGENTS.md - Moment Pic 项目开发规范

> 日期：2026-05-15  
> 执行者：Codex  
> 适用范围：项目根目录及所有子目录；若子目录存在更具体的 `AGENTS.md`，以子目录规范优先。

## 1. 项目定位

Moment Pic 是一个以图片、相册、缩略图、智能相册和媒体浏览体验为核心的应用项目。当前仓库采用 npm workspaces 管理多应用结构：

- `apps/server`：TypeScript + Fastify 服务端，负责 API、数据库、缩略图、媒体索引、WebSocket 等能力。
- `apps/web`：React + Vite 前端，负责图片浏览、相册交互、系统配置和用户界面。
- `apps/server-rs`：Rust 服务端实验或补充模块，使用 Axum/Tokio 生态。

所有改动必须先理解所属模块职责，再按既有分层和命名方式落地。

## 2. 智能体职责

| 智能体 | 职责 |
| --- | --- |
| Codex | 需求分析、方案设计、代码实现、验证、文档更新和交付总结 |
| Coder | 聚焦具体代码修改，遵循现有架构与本文件规范 |
| Tester | 复现问题、设计测试、执行本地验证并输出结论 |
| Media | 处理图片、缩略图、归档文件、媒体元数据等媒体域需求 |
| Streaming | 处理流式任务、长耗时任务和异步状态同步相关需求 |

## 3. 工作流程

1. 需求理解：明确目标、影响范围、输入输出、边界条件和验收方式。
2. 上下文阅读：至少阅读目标文件、调用方和 1 个相似实现；复杂功能应阅读 3 个相似实现。
3. 方案设计：优先复用现有服务、仓储、Hook、组件和类型定义，避免重复造轮子。
4. 小步实现：每次修改保持可构建、可回退、可解释。
5. 本地验证：按影响范围运行类型检查、构建、单元测试或冒烟测试。
6. 交付记录：说明修改内容、验证结果、剩余风险和后续建议。

遇到 Bug 时遵循 Loop-until-Success：

1. 复现问题并记录现象、日志和最小复现路径。
2. 定位根因，优先修复根因而不是掩盖症状。
3. 编写或补充回归测试。
4. 执行验证；若连续 3 次修复失败，暂停并重新评估方案。

## 4. 架构与分层规范

### 4.1 服务端 `apps/server`

- `routes` 只处理 HTTP/WebSocket 协议适配、参数解析和响应映射。
- `services` 承载业务用例，编排仓储、文件系统、图片处理和外部能力。
- `repositories` 封装数据访问，不向路由层暴露数据库细节。
- `db` 管理数据库连接、迁移或初始化逻辑。
- `types` 定义跨层 DTO、领域类型和 Fastify 扩展类型。
- `config` 只负责配置读取、默认值和环境变量解析。

禁止在路由中直接堆叠复杂业务逻辑；禁止在 UI 或 API 层直接拼装数据库语句。

### 4.2 前端 `apps/web`

- `components` 放可复用 UI 组件，组件应保持受控、可组合、低副作用。
- `hooks` 放可复用状态逻辑和浏览器能力封装。
- `app` 放页面级控制器、业务编排和应用状态协调。
- `lib` 放 API 客户端、纯函数、格式化、质量策略等通用能力。
- `types` 放前端 API 类型、领域类型和共享类型。

组件不要直接承担复杂业务编排；复杂流程优先下沉到 Hook、控制器或纯函数。

### 4.3 Rust `apps/server-rs`

- 使用 Rust 2021 风格，优先采用 Axum/Tokio 官方生态。
- 路由、状态、领域逻辑、序列化类型应分层清晰。
- 对可失败路径使用 `Result` 表达错误，不使用 `unwrap`/`expect` 处理业务错误。

## 5. 设计原则

所有新增或修改代码必须符合以下原则：

- 单一职责原则：一个函数、类、Hook 或服务只承担一个清晰职责。
- 开闭原则：新增行为优先通过扩展策略、组合和配置完成，避免修改稳定核心流程。
- 里氏替换原则：接口实现必须保持调用方可替换，不引入隐藏前置条件。
- 接口隔离原则：接口按使用场景拆分，避免大而全的参数对象和万能服务。
- 依赖倒置原则：高层业务依赖抽象能力，不直接依赖易变实现细节。
- DRY：重复出现 3 次以上的稳定逻辑应提取复用。
- KISS：选择可读、可测试、可维护的简单方案。
- YAGNI：没有明确需求和调用场景时，不提前扩展架构。

## 6. 设计模式使用要求

设计模式用于降低复杂度，不用于制造复杂度。满足以下场景时优先使用成熟模式：

- Strategy：图片质量策略、排序策略、筛选策略、扫描策略等可替换算法。
- Factory：根据文件类型、来源类型、归档类型创建处理器。
- Adapter：隔离第三方库、文件系统、图片处理、AI SDK 或浏览器 API。
- Repository：封装数据库访问，保持业务层不关心持久化细节。
- Observer / Pub-Sub：目录监听、扫描进度、WebSocket 状态推送。
- Command：封装可排队、可重试、可记录的长耗时任务。
- Facade：为复杂子系统提供稳定入口，如缩略图生成、智能相册重建。
- Builder：构造复杂查询条件、响应 DTO 或配置对象。

使用模式前必须满足：

- 该模式能明确减少条件分支、重复代码或跨层耦合。
- 命名体现业务语义，而不是仅体现模式名称。
- 测试覆盖主要策略、适配器或工厂分支。

禁止为了“使用设计模式”而引入无业务价值的抽象层。

## 7. TypeScript 代码规范

- 使用严格类型，避免 `any`；确需使用时必须说明边界并尽快收敛。
- 公共函数、服务方法和导出类型应有明确输入输出类型。
- 异步函数必须明确错误路径，避免吞掉异常。
- 使用 `import type` 引入纯类型。
- 优先使用纯函数处理可预测转换逻辑。
- 文件名采用项目既有 kebab-case 风格，如 `thumbnail-service.ts`。
- 测试文件与目标文件同目录，命名为 `*.test.ts`。
- Node 内置模块使用 `node:` 前缀。

注释遵循 Why > How：代码表达做什么，注释说明为什么这样做、边界是什么、修改风险是什么。

```typescript
/**
 * 根据图片来源创建统一读取入口。
 * @param source 图片来源描述
 * @returns 可读取的图片内容与元信息
 * @throws 当来源不存在或格式不受支持时抛出错误
 */
```

## 8. React 前端规范

- 组件保持小而清晰，优先通过 props 表达输入，通过事件回调表达输出。
- Hook 只封装状态和副作用，不返回不稳定的匿名复杂对象。
- UI 状态、服务端状态、派生状态要分离，避免同一份数据多处维护。
- API 调用集中放在 `lib/api.ts` 或相邻 API 模块。
- 图片浏览、手势、滚动、动画等交互必须考虑加载中、空状态、失败状态和移动端表现。
- 图标优先使用 `lucide-react`。
- 样式遵循现有 Tailwind/Vite 方案，不新增并行样式体系。

## 9. 服务端规范

- Fastify 路由必须声明清晰的请求参数、查询参数和响应结构。
- 业务流程放在 service；数据库读写放在 repository 或 store。
- 文件系统、压缩包、图片处理、外部进程调用必须封装为可测试边界。
- 长耗时任务必须能报告状态，避免阻塞请求主流程。
- WebSocket 推送必须定义消息类型，避免散落字符串。
- 日志使用中文描述业务语境，保留必要的错误对象和上下文标识。

日志格式建议：

```text
[时间戳] [模块名] [级别] 日志内容
```

## 10. 数据与接口规范

- DTO 与领域模型分离，接口响应不直接暴露数据库内部字段，除非该字段是明确契约。
- 时间字段统一使用 ISO 字符串。
- ID、路径、文件名、扩展名、来源类型等关键字段必须在类型层表达。
- 接口变更要同步更新前端类型和调用方。
- Prisma 或 SQLite 相关变更必须提供迁移或初始化兼容方案，并说明数据影响。

## 11. 媒体处理规范

- 图片处理优先复用 `sharp` 等成熟库，不自研编解码。
- 归档处理优先复用现有 7z、yauzl、node-unrar-js 相关封装。
- 缩略图、预览图、原图读取必须区分缓存键、源文件状态和生成策略。
- 对大文件、批量扫描、归档读取要避免一次性加载全部内容到内存。
- 媒体处理逻辑必须覆盖普通文件、归档内文件、缺失文件和异常格式。

## 12. 测试规范

优先沿用项目现有测试方式：

- 服务端 TypeScript 测试使用 `node:test` 与 `node:assert/strict`。
- 前端至少运行类型检查和构建。
- Rust 修改至少运行 `cargo test`。

常用验证命令：

```powershell
npm run build:server
npm run build --workspace @moment-pic/web
npm run lint --workspace @moment-pic/web
node --import tsx --test apps/server/src/**/*.test.ts
cargo test --manifest-path apps/server-rs/Cargo.toml
```

测试覆盖要求：

- 正常流程：核心业务能够按预期完成。
- 边界条件：空数据、缺失文件、异常输入、重复请求、缓存命中与失效。
- 错误恢复：外部库失败、文件不可读、数据库写入失败时有明确行为。
- 回归测试：每个 Bug 修复都应补充能失败后通过的测试。

## 13. 文档规范

- 行为变化、接口变化、部署方式变化必须同步更新文档。
- 文档使用中文，术语保持一致。
- 新增规范或设计说明应标注日期和执行者。
- 复杂方案应记录动机、约束、取舍和验证方式。
- 不保留过期说明；代码删除时同步删除对应文档。

## 14. Git 与提交规范

- 提交前检查 `git status`，确认只包含本任务相关文件。
- 不回滚他人或用户已有改动。
- 提交信息使用简洁中文或 Conventional Commits 风格。
- 大型改动拆分为可审查的小提交。
- 不提交本地私密配置、生成缓存、临时调试输出和大体积无关文件。

## 15. 质量门禁

交付前至少完成以下检查：

- 代码符合所属模块分层。
- 类型定义完整，调用方已同步。
- 没有无意义抽象、重复逻辑和遗留死代码。
- 关键路径有测试或明确的本地验证记录。
- 前端改动完成构建或类型检查；重要视觉改动完成浏览器验证。
- 服务端改动完成构建和相关测试。
- 媒体处理改动覆盖真实样例或构造样例。

## 16. 禁止事项

- 禁止绕过现有模块分层直接跨层调用。
- 禁止在业务代码中散落魔法字符串和未命名常量。
- 禁止新增未使用依赖、重复工具链或并行框架。
- 禁止用全局可变状态承载业务流程，除非有明确生命周期管理。
- 禁止提交只有占位、TODO 或不可运行的半成品实现。
- 禁止忽略 TypeScript、Rust 或构建错误后继续交付。

## 17. 交付格式

最终回复应包含：

- 修改概述。
- 影响文件。
- 已执行的验证命令和结果。
- 未执行验证的原因及风险。
- 如有必要，列出后续建议。
