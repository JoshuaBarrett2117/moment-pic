# Agents 配置

本文档定义了 Moment Pic 项目中使用的 AI 智能体配置与协作规范。

## 智能体列表

| 智能体 | 类型 | 用途 |
| :--- | :--- | :--- |
| Codex | project-analyzer | 需求分析与项目架构设计 |
| Coder | java-coder | 代码编写与修改 |
| Tester | tester | 测试验证与故障排查 |
| Media | media-coder | 媒体处理相关开发 |
| Streaming | Task (media-producter) | 流媒体与 PT 相关功能 |

## 协作工作流

### 1. 需求分析阶段

当接收到新需求时，使用 `project-analyzer` 智能体进行需求拆解与架构设计。

### 2. 开发实现阶段

使用 `java-coder` 或 `media-coder` 智能体进行代码编写。

### 3. 测试验证阶段

使用 `tester` 智能体进行功能测试与故障排查。

## 代码规范

### 注释规范

- **Why > How**：代码说明"怎么做"，注释说明"为什么这么做"
- **语义化优先**：变量名能解释清楚则不写注释
- **同步更新**：代码逻辑变动时，注释必须即时更新

### 注释结构

采用 Google 风格，包含输入、输出及异常。

```typescript
/**
 * 函数功能描述
 * @param param1 参数说明
 * @returns 返回值说明
 * @throws 异常说明
 */
```

### 关键逻辑标注

- **Step X**：复杂算法步骤标注
- **FIXME**：待修已知 Bug
- **TODO**：待办占位符
- **NOTE**：非标准实现说明
- **WARN**：警告修改此处可能导致的副作用

## 日志规范

- 统一使用中文输出日志
- 格式：`[时间戳] [模块名] 日志内容`
- 级别：INFO（正常流程）、DEBUG（调试）、ERROR（异常）

## 故障排查

遵循 "Loop-until-Success" 循环：

1. **排查阶段**：调用 `tester` 智能体分析根因
2. **修复阶段**：将分析结果传递给 `coder` 智能体
3. **验证阶段**：修复完成后再次调用 `tester` 回归测试
4. **循环判定**：超过 3 次未解决需人工介入

### 状态标记

- `[ACTION: INVESTIGATING]` - 正在调用 tester
- `[ACTION: FIXING]` - 正在调用 coder
- `[STATUS: LOOPING]` - 修复未通过，进入下一轮迭代

## 需求确认与异步 QA 协作规范

### 歧义挂起机制

当 Agent 接收到新需求或重构指令时，若发现业务逻辑存在歧义、边界情况不明或技术实现方案存在多种选择，**严禁**直接开始编写代码。

### 文档 QA 交互模式

Agent 必须停止当前开发流程，并在相关的 PRD 或技术文档末尾新增 **[需求答疑区]**。交互格式如下：

- **[question]**：Agent 针对需求的具体疑问或需要用户确认的场景
- **[answer]**：保持留白，等待用户填写

### 准入开发红线

- **多轮迭代**：支持多轮 QA 往复，直到文档中所有 `[question]` 均得到明确答复
- **开发准则**：只有当所有潜在风险和逻辑歧义均被消除、且用户确认方案可行后，Agent 才可以转入具体的代码重构与开发阶段
