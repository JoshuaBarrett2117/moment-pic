# 实现契约补充说明

日期：2026-04-09
执行者：Codex

## 1. 本次补充

基于最新讨论，第一阶段图片格式支持不应只限于 `jpg`。

当前代码骨架里，图片扩展名白名单已经按通用图片格式定义为：

1. `jpg`
2. `jpeg`
3. `png`
4. `webp`
5. `gif`
6. `bmp`

对应代码位置：

1. [apps/server/src/lib/image-formats.ts](C:\Users\a3875\Documents\code\moment-pic\apps\server\src\lib\image-formats.ts)

## 2. 本次落地内容

已完成第一版后端契约骨架：

1. `Prisma schema`
2. `Fastify` 路由骨架
3. DTO 类型定义
4. API 返回结构
5. 示例数据与占位处理器

关键文件：

1. [apps/server/prisma/schema.prisma](C:\Users\a3875\Documents\code\moment-pic\apps\server\prisma\schema.prisma)
2. [apps/server/src/routes/albums.ts](C:\Users\a3875\Documents\code\moment-pic\apps\server\src\routes\albums.ts)
3. [apps/server/src/routes/assets.ts](C:\Users\a3875\Documents\code\moment-pic\apps\server\src\routes\assets.ts)
4. [apps/server/src/types/dto.ts](C:\Users\a3875\Documents\code\moment-pic\apps\server\src\types\dto.ts)

## 3. 当前状态

这套骨架的目标是固定第一阶段的数据契约和接口形状，还没有进入真实扫描、入库、缩略图生成和图片流读取实现。

也就是说，现在已经有：

1. 可继续编码的项目结构
2. 可讨论和调整的数据库模型
3. 可直接替换成真实服务实现的路由层

还没有：

1. Prisma migration
2. 数据库访问层
3. 目录扫描器
4. ZIP 扫描器
5. 缩略图服务
6. 原图读取服务

